import { createCommandGroupDecorator } from 'necord';

export const GameCommandGroup = createCommandGroupDecorator({
  name: 'world',
  description: 'Раздел команд, связанных с Мировым господством',
});
